---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - argocd-app.yaml
  - piraeus-operator-app.yaml
  # piraeus-datastore-app.yaml is a stub (comment-only).
  # Add it here when Task 8 replaces it with a real Application CR.
