---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - argocd-app.yaml
  # piraeus-operator-app.yaml and piraeus-datastore-app.yaml are stubs (comment-only).
  # Add them here when Tasks 7 and 8 replace them with real Application CRs.
