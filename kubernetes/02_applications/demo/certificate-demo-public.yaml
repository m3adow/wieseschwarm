---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: demo-public-tls
  namespace: demo
spec:
  secretName: demo-public-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - demo.wieseclan.eu.org
